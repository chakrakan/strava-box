version: 2.1

defaults: &defaults
  working_directory: ~/repo
  docker:
    - image: circleci/node:13

commands:
  install:
    steps:
      - checkout
      - restore_cache:
          keys:
            - dependency-cache-updated-{{ checksum "package.json" }}
            - dependency-cache-updated
      - run: npm install
      - save_cache:
          key: dependency-cache-updated-{{ checksum "package.json" }}
          paths:
            - node_modules

jobs:
  stravabox:
    <<: *defaults
    steps:
      - install
      - restore_cache:
          keys:
            - strava-auth-updated-{{ .Environment.CIRCLE_PREVIOUS_BUILD_NUM }}
            - strava-auth-updated
      - run: npm start
      - save_cache:
          key: strava-auth-updated-{{ .Environment.CIRCLE_BUILD_NUM }}
          paths:
            - strava-auth-updated.json

workflows:
  version: 2

  test:
    jobs:
      - stravabox

  poll:
    triggers:
      - schedule:
          # Run once nightly at 10pm
          cron: "00 2 * * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - stravabox
